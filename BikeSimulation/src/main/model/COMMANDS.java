package main.model;

/**
 * allows validation against correct command names.
 * If a new command to be introduced, an entry needs to be added here
 */
public enum COMMANDS {
    PLACE, TURN_LEFT, TURN_RIGHT, GPS_REPORT, FORWARD
}
